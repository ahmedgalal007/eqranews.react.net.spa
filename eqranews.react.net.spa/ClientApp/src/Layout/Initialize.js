export const InELEMENT = { URL: '', SELECTOR: '' };

export const IntializeLayout = (pageScripts = [], pageCSS = []) => {
	return (new Promise(resolve, reject) = () => {});
	for (const script in object) {
		if (object.hasOwnProperty(key)) {
			const element = object[key];
		}
	}
};
